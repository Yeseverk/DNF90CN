# DNF90CN 开发环境

## 进入 90CN

### 客户端

将以下五个文件替换到客户端根目录：

- `DNF.exe`
- `90CN.dll`
- `90CNLua.dll`
- `ijl15.dll`
- `ijl15_real.dll`

### 服务端

1. 双击运行 `LOGIN.bat`。
2. 首次运行时选择客户端目录中的 `DNF.exe`。
3. 新账号点击“注册并进入”；已有账号点击“进入游戏”。

客户端与服务端的 `Script.pvf`、`.lst` 注册和对应服务端实现必须保持一致。
